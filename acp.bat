@echo off
rem ======================================================
rem 武器刻寫規劃器 - 自動提交工具 (ACP)
rem ======================================================

rem [1] git add .
rem 原理：將當前目錄下所有變動（新增、修改、刪除）加入暫存區。
git add .

rem [2] git commit -m "%*"
rem 原理：提交變更。
rem 關鍵修改：將原本的 %1 改為 %*。
rem %1 只會抓取第一個空格前的單字。
rem %* 會抓取後方「所有」輸入內容，因此你打 acp fix bug 不再需要加引號。
git commit -m "%*"

rem [3] git push
rem 原理：將本地分支的更新推送到 GitHub。
git push

rem ======================================================
rem 使用說明：
rem CMD: 直接輸入 acp 你的訊息
rem PowerShell: 直接輸入 .\acp 你的訊息 (因安全性限制需加 .\)
rem ======================================================
